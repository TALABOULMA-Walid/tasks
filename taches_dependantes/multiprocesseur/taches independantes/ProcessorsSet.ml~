value newParSolution listProc idTask- idProc =
let listP = ref list [] in
 for i=0 to listProc.length do {
   listP:= (!listP) @ (List.nth listProc i) ;
   if i=idProc do {
    (List.nth listP i).listTasks-<(List.nth listP i).listTasks @[idTask] in                 
   }
    
}

